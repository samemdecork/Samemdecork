# صمم ديكورك — Premium Preview

ضع `index.html` و`style.css` ومجلد `assets` في نفس المكان داخل GitHub Pages.

الصور المرفوعة من أعمالك موجودة داخل `assets` بأسماء work-1 إلى work-10.

النسخة الحالية Front-end: رفع الصورة ومعطيات الطلب تعمل داخل الواجهة، والإرسال يفتح واتساب.
الذكاء الاصطناعي الحقيقي، قاعدة بيانات الحسابات، ولوحة التحكم للإكسسوارات تحتاج Backend/API لاحقاً.
